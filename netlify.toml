# Netlify 배포 설정 — Next.js(App Router) on Netlify
# @netlify/plugin-nextjs(Next Runtime)가 SSR/SSG 라우트를 처리합니다.

[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
